name: "first workflow"
on: push
jobs:
    first-job:
      runs-on: windows-latest
      steps:
         - name: checkout
           uses: actions/checkout@v3
           with:
             node-version: 24
             cache: npm
         - name: install dependency
           run: npm ci
         
         - name: make react build
           run: npm run build     
           